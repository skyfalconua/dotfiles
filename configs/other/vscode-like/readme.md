### install extensions

```sh
  for e in `cat user-settings/extensions.txt`; do code --install-extension $e; done

  for e in `cat user-settings/extensions.txt`; do  antigravity --install-extension $e; done
```
